# Field Notes

This is a representative **Markdown document** used to check Chwezi Markdown Reader’s rendering, navigation, and rich-copy behaviour.

## Today’s decisions

- Keep the reading surface quiet and paper-like.
- Preserve headings, tables, links, lists, and code when copied.
- [x] Register Markdown file extensions in the Windows installer.

> A useful reader should disappear behind the document.

## Delivery table

| Feature | Status | Shortcut |
| --- | --- | --- |
| Open file | Ready | `Ctrl+O` |
| Find text | Ready | `Ctrl+F` |
| Copy rendered | Ready | `Ctrl+Shift+C` |

## Code sample

```ts
const message = "Markdown, rendered locally";
console.log(message);
```

See the [project repository](https://example.com) for supporting files.
